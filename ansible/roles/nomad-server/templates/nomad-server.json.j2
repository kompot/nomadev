{
  "service": {
    "name": "nomad-server",
    "tags": ["infrastructure"],
    "port": {{ nomad_ports_rpc }},
    "check": {
      "name": "alive-nomad-tcp-check",
      "tcp": "{{ ansible_host }}:{{ nomad_ports_rpc }}",
      "interval": "10s",
      "timeout": "2s"
    }
  }
}
